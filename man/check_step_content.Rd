% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_protocol_agmip.R
\name{check_step_content}
\alias{check_step_content}
\title{Check the content of sub-steps for forbidden elements}
\usage{
check_step_content(step, forbidden_elements = c("obs_list", "transform_obs"))
}
\arguments{
\item{step}{A list of sub-steps. Each sub-step should itself be a named list
describing a group of variables (e.g., obs_var, major_param, candidate_param).}

\item{forbidden_elements}{A character vector of element names that are not allowed
in sub-steps. Default is \code{c("obs_list", "transform_obs")}.}
}
\value{
Invisibly returns \code{TRUE} if all checks pass. Throws an error if any
forbidden element is found.
}
\description{
This function validates that none of the sub-steps in a step list contain elements
that are forbidden, such as \code{obs_list} or \code{transform_obs}. These elements must be
passed directly as arguments to \code{run_protocol_agmip} rather than within a sub-step.
}
\examples{
# Valid step
step_ok <- list(
  list(obs_var = c("var1"), major_param = c("p1"))
)
\dontrun{
check_step_content(step_ok)
}

# Invalid step
step_err <- list(
  list(obs_var = c("var1"), obs_list = list(a = 1))
)
\dontrun{
check_step_content(step_err) # triggers an error
}

}
\keyword{internal}
